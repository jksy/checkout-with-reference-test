name: on-push

on: push

jobs:
  test-on-push:
    runs-on: ubuntu-latest
    steps:
      - run: echo "executed"

      - name: debug
        shell: bash
        run: |
          set -xe

          echo github context '${{ github }}'
          env

      - uses: jksy/checkout-with-reference@2c4b178bf6db494e60262dc9bc0220e2101a57d5
        with:
          github_token: ${{ github.token }}
          github_actor: ${{ github.actor }}
          github_repository: ${{ github.repository }}
          reference_dir: /not-exists
          checkout_dir: .
